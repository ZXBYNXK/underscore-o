const {_O} = 
import api from './api';
import database from './database';
import models from './models';
import server from './server';

cosole.log(_O({
    server,
    database,
    api,
    models,
}))